# Experimental Connectors

This directory contains experimental connectors under active evaluation.

Experimental connectors are not part of the official connector set and are
not included as pnpm workspaces until an actual connector package is
introduced.

Experimental connectors must not be treated as supported official
integrations unless they are explicitly promoted through the governed
change process.
